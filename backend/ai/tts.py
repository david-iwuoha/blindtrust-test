# backend/ai/tts.py

def text_to_speech(text: str, language_code: str = "en") -> str:
    """
    Placeholder TTS function.
    Converts text to audio file (future implementation will use a TTS engine).
    
    For now, returns a placeholder filename.
    """
    # Example: we could save to 'output.mp3' in future
    return "placeholder_audio.mp3"
